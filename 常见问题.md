# 常见问题（FAQ）

## 1. 持仓或下单失败怎么办？
- **`-2019 margin is insufficient`**：策略根据账户净值动态放大仓位。如果频繁触发，请调低 `quant_config.position_size_pct` 或增大初始余额。
- **`Order's position side does not match user's setting`**：确认 Binance 永续账户已启用双向持仓（Hedge Mode）。
- **交易频次过高**：可以设置 `quant_config.cooldown_minutes`，或增大 `scan_interval_minutes`。

## 2. 如何扩展候选币种？
- 默认会加载 `config.json` 中的 `default_coins` 与 `additional_coins`。
- 如需动态获取，可配置 `coin_pool_api_url` 或 `oi_top_api_url`，返回形如 `{"symbols": ["BTCUSDT","BTCUSDC", ...]}` 的 HTTP 接口。

## 3. 重启后如何恢复上下文？
- 程序启动会自动同步交易所持仓并匹配最近的决策记录。
- 如发现持仓列表与日志不一致，可调用 `POST /diagnostics/sync-positions`（前端“同步持仓”按钮）。

## 4. 日志/数据存放在哪？
- `decision_logs/<trader_id>/`：每个周期的账户快照、候选币种、执行动作。
- `data/exchange_trades/<trader_id>.json`：聚合后的开平仓记录，用于前端“历史订单”。
- `data/position_history/<trader_id>.json`：最近几百笔交易结果，供复盘脚本使用。

## 5. 调整前端行情对列表？
- 设置环境变量 `NEXT_PUBLIC_TICKER_PAIRS=BTCUSDT,BTCUSDC,ETHUSDT,ETHUSDC,SOLUSDT,...`，然后重启前端。

## 6. 只需要监控，不想自动开仓？
- 可以把 `quant_config.position_size_pct` 设为非常小的值（如 `0.001`）并在交易所侧设置最小下单金额，或直接在 `traders[0].enabled` 设为 `false`，只保留前端监控功能。
