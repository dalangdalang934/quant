# 常见问题 FAQ

## 如何切换逐仓 / 全仓？

系统默认使用 **逐仓 (isolated)** 保证金模式。如果需要改用 **全仓 (cross)**，请按照下列步骤操作：

1. 打开部署目录下的 `config.json`（或对应环境使用的配置文件）。
2. 在需要调整的 Trader 配置中，找到 `margin_type` 字段：

   ```json
   {
     "id": "binance_deepseek",
     "name": "Binance DeepSeek",
     "enabled": true,
     "ai_model": "deepseek",
     "exchange": "binance",
     "margin_type": "isolated",   // 默认逐仓
     ...
   }
   ```

3. 将 `margin_type` 改为 `cross` 即可启用全仓模式：

   ```json
   "margin_type": "cross"
   ```

4. 保存配置后，重启后端服务（或在 Docker 部署中执行 `docker compose up -d`）让新配置生效。

### 注意事项

- 仅对 **币安合约** 有效，其他交易所会忽略此字段。
- 支持的取值：`isolated`（逐仓，默认）、`cross`（全仓）。配置时大小写不敏感，系统会自动规范化。
- 切换模式会在开仓前调用币安 API 动态设置，关闭容器或重启服务后仍会按最新配置执行。
- 若使用全仓，请确保账号风险控制充足，避免不同仓位互相拖累。

## AI 学习与反思是如何工作的？

- **历史数据来源**：系统会持续写入 `decision_logs/`（AI 决策记录）与 `data/exchange_trades/`（交易所成交回放）。每日不要手动删除，否则会影响策略恢复、绩效统计和前端展示。
- **分析窗口**：后台在每个扫描周期读取大量（约 5000 个周期）的日志数据，合成 `PerformanceAnalysis`，包含夏普比率、胜率、盈亏比、币种表现、最近 20 笔交易等。
- **学习状态生成**：`learning/state.go` 会把最新绩效压缩成“学习状态”（`data/learning_state/<trader_id>.json`），其中包括：
  - 风险指令：最低置信度、最大持仓数、仓位系数、冷静期等；表现欠佳时会自动收紧（例如最大持仓降为 1）。
  - 币种指引：按胜率 / 盈亏将币种标记为 `focus` / `watch` / `avoid`，并写上理由。
  - 执行约束：建议的最小持仓时间、每小时可开仓次数提醒等。
  - 洞察摘要：告诉 AI 最近表现问题、改进方向。
- **决策阶段使用**：在 `buildUserPrompt` 中会把这些指令嵌入到 AI Prompt；`applyLearningConstraints` 则在执行前再次检查。如果某笔交易被拒绝（例如超出最大持仓、置信度不足、币种被标记为避开），后台日志会打印详细原因。
- **调优建议**：
  - 保留至少 7~30 天的决策日志，以便 AI 有稳定的历史可参考。
  - 若想让 AI 更关注最新行情，可缩短学习状态的统计窗口或对旧交易加权衰减，而不是删除原始日志。
  - 观察 `learning_state/*.json`，即可了解当前风险指令与黑白名单。

## 如何自定义候选代币？

系统默认使用 `config.json` 中的 `default_coins` 列表（或外部 AI500/OI 接口）作为候选池，并支持手动维护：

1. 打开部署目录的 `config.json`，在根级别找到 `default_coins`。
2. 按需追加或删除币对，使用交易所标准交易对（如 `ARBUSDT`、`OPUSDT`）。例如：
   ```json
   "default_coins": [
     "BTCUSDT",
     "ETHUSDT",
     "SOLUSDT",
     "ARBUSDT",
     "OPUSDT"
   ]
   ```
3. 确保 `use_default_coins` 为 `true`，否则系统会只使用外部候选池。
4. 保存后重启后端（或 `docker compose up -d`）即可生效。

> 额外提示：
> - 若 `coin_pool_api_url` 或 `oi_top_api_url` 有配置，默认会把这些来源与 `default_coins` 合并去重。
> - 新增币种必须是交易所支持的 USDT 合约交易对；若是其他计价，需要在后台 Trader 实现里额外适配。
> - 观察后台日志确认新的“合并币种池”数量有增加，表示加载成功。

## 可以删除周期日志 / 学习数据吗？

可以，但建议 **先归档再清空**，避免直接丢失原始证据。

### 推荐流程：`scripts/archive-logs.sh`

1. 在项目根目录执行：

   ```bash
   ./scripts/archive-logs.sh <trader_id>
   ```

   举例：`./scripts/archive-logs.sh binance_deepseek`。

2. 脚本会把 `decision_logs/<trader_id>/`、`data/exchange_trades/<trader_id>.json`、`data/learning_state/<trader_id>.json`、`data/position_history/<trader_id>.json` 打包成 `archives/<trader_id>_YYYYMMDD_HHMMSS.tar.gz`，随后删除原始文件并重建空目录。
3. 归档完成后，重启后端（裸机：重新运行可执行文件；Docker：`docker compose up -d backend`）。
4. 启动日志会出现 `历史日志与学习数据为空，AI 将从头积累数据...`，同时 Prompt 中也会提示“当前绩效统计仅供参考”。后续策略评估要留意统计被重置。

### 删除后的影响

- **功能不会炸**：系统下一个周期会重新写入新的日志与学习状态。
- **历史上下文清零**：
  - 前端的盈亏统计、学习与反思列表会从零开始，需要等待新的交易产生。
  - 被动平仓检测与仓位恢复暂时失效，至少积累一整个周期后才能恢复参考价值。
  - 学习状态因为样本不足会更激进，最大持仓、置信度阈值等可能被大幅收紧。
  - 仓位历史（`data/position_history/<trader_id>.json`）用于持久化AI的交易记录，重启后仍可查看历史交易。
- **策略评估须知**：删除后的一段时间内，所有绩效图表都不再可比，需重新积累数据再做判断。
- **重启行为**：即使不删除文件，每次重启后台都会重新扫描日志并生成最新学习状态；保留历史可以让新状态继承原有统计。

### 手动清理路径（脚本不可用时）

```bash
rm -rf decision_logs/<trader_id>/
rm -f data/exchange_trades/<trader_id>.json
rm -f data/learning_state/<trader_id>.json
rm -f data/position_history/<trader_id>.json
```

删除后务必重启后端，并关注后台日志确认 AI 已开始重新积累数据。

## 裸机部署（替代 Docker）指南

> **使用 `screen` 的简易替代**：如果只是临时运行，也可以用 screen 直接开启两个会话：
> ```bash
> screen -S jiaoyibot-backend
> cd /opt/jiaoyibot
> ./nofx config.json
> ```
> ```bash
> screen -S jiaoyibot-frontend
> cd /opt/jiaoyibot/web
> npm run start -- --hostname 0.0.0.0 --port 3000
> ```
> 然后 `Ctrl+A D` 挂起，`screen -r <name>` 恢复。虽然简单，但没有自动重启和日志管理功能，长期部署仍推荐 systemd/pm2。

### Next.js / lightningcss 依赖缺失

在 Linux 服务器上通过 `screen` 或裸机直接运行前端时，如果以前在另一台机器（如 macOS）安装过 `node_modules`，会出现以下报错：

```
Error: Cannot find module '../lightningcss.linux-x64-gnu.node'
```

这是因为 `lightningcss` 带有平台相关的原生二进制，必须 **在目标服务器上重新安装依赖**。解决办法：

1. 进入前端目录并删除旧的本地依赖：
   ```bash
   cd /path/to/jiaoyibot/web
   rm -rf node_modules package-lock.json
   npm install
   ```
2. Debian/Ubuntu 环境（glibc）建议先安装编译工具，再重新安装依赖：
   ```bash
   sudo apt-get update
   sudo apt-get install -y build-essential python3
   npm install
   ```
3. 如果使用的是 Alpine（musl），需要额外安装编译工具：
   ```bash
   apk add --no-cache build-base python3
   npm install
   ```
4. 如仍提示缺少二进制，可尝试重建：
   ```bash
   npm rebuild lightningcss --verbose
   ```
5. 临时方案：设置 `LIGHTNINGCSS_WASM=1` 改用 WASM 版本（性能略逊）。

总之，**不要复制其它平台生成的 `node_modules` 到服务器**；务必在目标环境执行 `npm install`，再配合 `screen` 启动即可避免该错误。

## 如何修改 AI 提示词？

系统内置的交易提示词（Prompt）存放在 `decision/templates/system_prompt.tmpl` 中，修改方式如下：

1. 打开仓库根目录的 `decision/templates/system_prompt.tmpl`，找到需要调整的段落。文件上半部分是固定指令，下半部分包含参数化内容（例如 `{{.AccountEquity}}`、`{{.SampleBTCLeverage}}` 等），请保留这些占位符。
2. 按需修改文本、增加/删减规则或提示。只要保持模板语法不破坏，后台会在生成 Prompt 时自动替换变量。
3. 保存后**必须重启后端服务**（或重新执行可执行文件 / `docker compose up -d`），新 Prompt 才会在下一次 AI 调用时生效。
4. 若希望在不同环境使用不同模板，可复制 `system_prompt.tmpl` 成多个版本，并在配置文件中调整加载路径（进阶用法需同步修改代码）。

> 建议修改前先备份原文件，避免模板语法误改导致后台无法渲染 Prompt。每次更新提示词后，可关注后台日志确认系统成功输出新的 Prompt 片段，以验证修改已生效。
